#### Manuel



Dans le dossier "panthomScreenshot"

```bash
# installer phantomjs
sudo apt install phantomjs

# se placer dans le dossier phantomScreenshot
npm install

# lancer avec
sudo node index.js

# l'application se lancera sur localhost:3376
```
